import { useCallback, useEffect, useMemo, useState } from 'react'
import { useOutletContext } from 'react-router-dom'
import { supabase } from '../../lib/supabase.js'
import { formatMoney } from '../../lib/money.js'
import { paymentEventAmount } from '../../lib/paymentMoney.js'
import PaymentModal from './PaymentModal.jsx'

export default function PaymentsPage(){
  const {event}=useOutletContext(); const [payments,setPayments]=useState([]); const [items,setItems]=useState([]); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); const [filter,setFilter]=useState('all'); const [modal,setModal]=useState(false); const [editing,setEditing]=useState(null)
  const loadAll=useCallback(async()=>{setLoading(true);setError('');const [paymentRes,itemRes,assignmentRes]=await Promise.all([supabase.from('vendor_payments').select('*').eq('event_id',event.id).order('due_date',{ascending:true,nullsFirst:false}),supabase.from('budget_items').select('id,description,category_id,event_vendor_id').eq('event_id',event.id).order('description'),supabase.from('event_vendors').select('id,currency,exchange_rate,exchange_rate_source,exchange_rate_date,vendors(company_name)').eq('event_id',event.id)]);const firstError=paymentRes.error||itemRes.error||assignmentRes.error;if(firstError)setError(firstError.message);const vendorMap=new Map((assignmentRes.data||[]).map(a=>[a.id,a]));setPayments(paymentRes.data||[]);setItems((itemRes.data||[]).map(i=>{const a=vendorMap.get(i.event_vendor_id);return{...i,vendor_name:a?.vendors?.company_name||'',vendor_currency:a?.currency||event.currency,vendor_exchange_rate:a?.exchange_rate||1,vendor_exchange_rate_source:a?.exchange_rate_source||'',vendor_exchange_rate_date:a?.exchange_rate_date||''}}));setLoading(false)},[event.id]);useEffect(()=>{loadAll()},[loadAll])
  const today=new Date().toISOString().slice(0,10);const rows=useMemo(()=>payments.map(p=>({...p,effectiveStatus:effectiveStatus(p,today),eventValue:paymentEventAmount(p,event.currency)})),[payments,today,event.currency]);const visible=rows.filter(r=>filter==='all'||r.effectiveStatus===filter);const paid=sum(rows.filter(r=>r.status==='paid').map(r=>r.eventValue));const overdue=rows.filter(r=>r.effectiveStatus==='overdue');const dueSoon=rows.filter(r=>r.status==='pending'&&r.due_date&&r.due_date>=today&&daysBetween(today,r.due_date)<=30);const pending=sum(rows.filter(r=>['pending','overdue'].includes(r.effectiveStatus)).map(r=>r.eventValue));const itemMap=new Map(items.map(i=>[i.id,i]));const currency=event.currency||'USD'
  async function markPaid(payment){const {error:updateError}=await supabase.from('vendor_payments').update({status:'paid',paid_at:new Date().toISOString()}).eq('id',payment.id).eq('event_id',event.id);if(updateError)setError(updateError.message);else loadAll()}
  async function removePayment(payment){if(!window.confirm(`¿Eliminar el pago “${payment.description||'sin concepto'}”?`))return;const {error:deleteError}=await supabase.from('vendor_payments').delete().eq('id',payment.id).eq('event_id',event.id);if(deleteError)setError(deleteError.message);else loadAll()}
  return <section className="payments-page"><div className="module-heading"><div><p className="eyebrow">PAGOS</p><h2>Pagos del evento</h2><p>Vista financiera global. Cada pago conserva su moneda original y se consolida en {event.currency} para Presupuesto.</p></div><button className="primary-btn" onClick={()=>{setEditing(null);setModal(true)}}>+ Nuevo pago</button></div><div className="linked-flow-banner">Proveedor contratado → plan de pagos → vencimientos → Presupuesto pagado/pendiente</div><div className="planning-metrics payment-metrics"><Metric label="Pagado" value={formatMoney(paid,currency)} detail="Equivalente confirmado"/><Metric label="Pendiente" value={formatMoney(pending,currency)} detail="Compromisos abiertos"/><Metric label="Por vencer" value={dueSoon.length} detail="Próximos 30 días"/><Metric label="Vencidos" value={overdue.length} detail={overdue.length?'Requieren seguimiento':'Todo al día'} danger={overdue.length>0}/></div><div className="payment-filter"><button className={filter==='all'?'active':''} onClick={()=>setFilter('all')}>Todos</button><button className={filter==='pending'?'active':''} onClick={()=>setFilter('pending')}>Pendientes</button><button className={filter==='overdue'?'active':''} onClick={()=>setFilter('overdue')}>Vencidos</button><button className={filter==='paid'?'active':''} onClick={()=>setFilter('paid')}>Pagados</button></div>{error&&<p className="form-error">{error}</p>}{loading?<div className="panel loading-panel">Cargando pagos…</div>:visible.length?<div className="payment-table-wrap"><table className="payment-table"><thead><tr><th>Concepto</th><th>Proveedor / presupuesto</th><th>Fecha pactada</th><th>Importe original</th><th>Equivalente</th><th>Estado</th><th>Medio</th><th></th></tr></thead><tbody>{visible.map(payment=>{const item=itemMap.get(payment.budget_item_id);return <tr key={payment.id} className={payment.effectiveStatus==='overdue'?'is-overdue':''}><td><strong>{payment.description||'Pago'}</strong>{payment.receipt_url&&<a href={payment.receipt_url} target="_blank" rel="noreferrer">Ver comprobante</a>}</td><td>{item?.vendor_name&&<strong>{item.vendor_name}</strong>}<small>{item?.description||'Sin vincular'}</small></td><td>{payment.due_date?formatDate(payment.due_date):'Sin fecha'}</td><td><strong>{formatMoney(payment.amount,payment.currency||currency)}</strong>{payment.currency!==currency&&payment.exchange_rate&&<small>TC {Number(payment.exchange_rate).toLocaleString('es-PY')}</small>}</td><td><strong>{formatMoney(payment.eventValue,currency)}</strong></td><td><span className={`payment-status payment-status-${payment.effectiveStatus}`}>{statusLabel(payment.effectiveStatus)}</span></td><td>{payment.payment_method||'—'}</td><td><div className="payment-actions">{payment.status!=='paid'&&payment.status!=='canceled'&&<button className="text-action" onClick={()=>markPaid(payment)}>Marcar pagado</button>}<button className="text-action" onClick={()=>{setEditing(payment);setModal(true)}}>Editar</button><button className="text-action danger-action" onClick={()=>removePayment(payment)}>Eliminar</button></div></td></tr>})}</tbody></table></div>:<div className="panel empty-state"><p className="eyebrow">PAGOS</p><h2>Todavía no hay pagos</h2><p>Al contratar desde Proveedores podés crear el plan automáticamente, o cargar pagos manuales acá.</p><button className="primary-btn" onClick={()=>{setEditing(null);setModal(true)}}>Crear primer pago</button></div>}<PaymentModal open={modal} event={event} items={items} payment={editing} onClose={()=>setModal(false)} onSaved={async()=>{setModal(false);await loadAll()}}/></section>
}
function Metric({label,value,detail,danger}){return <article className={`planning-metric ${danger?'danger':''}`}><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>}
function effectiveStatus(payment,today){if(payment.status==='paid'||payment.status==='canceled')return payment.status;if(payment.due_date&&payment.due_date<today)return'overdue';return'pending'}
function statusLabel(v){return({paid:'Pagado',pending:'Pendiente',overdue:'Vencido',canceled:'Cancelado'})[v]||v}
function sum(values){return values.reduce((a,v)=>a+(Number(v)||0),0)}
function formatDate(value){return new Intl.DateTimeFormat('es-PY',{day:'2-digit',month:'short',year:'numeric',timeZone:'UTC'}).format(new Date(`${value}T00:00:00Z`))}
function daysBetween(from,to){return Math.ceil((new Date(`${to}T00:00:00Z`)-new Date(`${from}T00:00:00Z`))/86400000)}
